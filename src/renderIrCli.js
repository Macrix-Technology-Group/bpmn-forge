import fs from 'fs'; import { normalizeIr } from './normalizer.js'; import { renderSvg } from './svgRenderer.js'; import { verifyIr } from './pipeline.js'; const input=process.argv[2], output=process.argv[3]||'output/diagram.svg'; fs.mkdirSync(output.substring(0, output.lastIndexOf('/'))||'.',{recursive:true}); const result=verifyIr(normalizeIr(JSON.parse(fs.readFileSync(input,'utf8')))); fs.writeFileSync(output, renderSvg(result.normalized,result.report)); fs.writeFileSync(output.replace(/\.svg$/,'.report.json'), JSON.stringify(result.report,null,2)); console.log(`Wrote ${output}`);
