import fs from 'fs'; import { buildExecutionManifest } from './executionManifest.js'; import { normalizeIr } from './normalizer.js'; const input=process.argv[2], output=process.argv[3]||'output/execution_manifest.json'; fs.mkdirSync(output.substring(0,output.lastIndexOf('/'))||'.',{recursive:true}); fs.writeFileSync(output,JSON.stringify(buildExecutionManifest(normalizeIr(JSON.parse(fs.readFileSync(input,'utf8')))),null,2)); console.log(`Wrote ${output}`);
