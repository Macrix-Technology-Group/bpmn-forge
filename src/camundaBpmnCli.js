import fs from 'fs'; import { exportBpmnXml } from './bpmnXmlExporter.js'; import { normalizeIr } from './normalizer.js'; const input=process.argv[2], output=process.argv[3]||'output/camunda.bpmn'; fs.mkdirSync(output.substring(0,output.lastIndexOf('/'))||'.',{recursive:true}); fs.writeFileSync(output,exportBpmnXml(normalizeIr(JSON.parse(fs.readFileSync(input,'utf8'))),{camunda:true})); console.log(`Wrote ${output}`);
