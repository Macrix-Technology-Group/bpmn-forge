import fs from 'fs'; import { importBpmnXml } from './bpmnXmlImporter.js'; import { normalizeIr } from './normalizer.js'; const input=process.argv[2], output=process.argv[3]||'output/imported.ir.json'; fs.mkdirSync(output.substring(0, output.lastIndexOf('/'))||'.',{recursive:true}); fs.writeFileSync(output, JSON.stringify(normalizeIr(importBpmnXml(fs.readFileSync(input,'utf8'))),null,2)); console.log(`Wrote ${output}`);
