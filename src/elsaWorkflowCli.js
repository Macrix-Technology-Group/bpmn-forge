import fs from 'fs'; import { irToElsaWorkflow } from './elsaWorkflowGenerator.js'; import { normalizeIr } from './normalizer.js'; const input=process.argv[2], output=process.argv[3]||'output/elsa_workflow.json'; fs.mkdirSync(output.substring(0,output.lastIndexOf('/'))||'.',{recursive:true}); fs.writeFileSync(output,JSON.stringify(irToElsaWorkflow(normalizeIr(JSON.parse(fs.readFileSync(input,'utf8')))),null,2)); console.log(`Wrote ${output}`);
