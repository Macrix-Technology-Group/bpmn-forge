import fs from 'fs'; import { normalizeIr } from './normalizer.js'; import { renderSvg } from './svgRenderer.js'; const input=process.argv[2], output=process.argv[3]||'output/semantic.svg'; fs.mkdirSync(output.substring(0,output.lastIndexOf('/'))||'.',{recursive:true}); fs.writeFileSync(output, renderSvg(normalizeIr(JSON.parse(fs.readFileSync(input,'utf8'))))); console.log(`Wrote ${output}`);
