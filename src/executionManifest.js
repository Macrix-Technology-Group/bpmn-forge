export function buildExecutionManifest(ir){const p=ir.process; const handlers=(p.nodes||[]).filter(n=>n.execution).map(n=>({nodeId:n.id,name:n.name,type:n.type,subtype:n.subtype,...n.execution})); return {processId:p.id,processName:p.name,isExecutable:Boolean(p.isExecutable),handlers,serviceTasks:handlers.filter(h=>h.subtype==='service'),userTasks:(p.nodes||[]).filter(n=>n.type==='task'&&n.subtype==='user').map(n=>({nodeId:n.id,name:n.name,...(n.execution||{})})),edges:p.edges||[]};}
