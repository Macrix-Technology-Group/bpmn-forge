function activityType(n){if(n.type==='event'&&n.subtype==='start')return 'Start'; if(n.type==='event'&&n.subtype==='end')return 'Finish'; if(n.type==='gateway')return 'FlowDecision'; if(n.type==='task'&&n.subtype==='user')return 'UserTask'; if(n.type==='subprocess')return 'RunWorkflow'; return 'RunTask';} export function irToElsaWorkflow(ir){const p=ir.process; return {id:p.id,name:p.name,version:1,activities:(p.nodes||[]).map(n=>({id:n.id,type:activityType(n),displayName:n.name,properties:{...(n.execution||{}),eventDefinition:n.event_definition,loop:n.loop}})),connections:(p.edges||[]).map(e=>({id:e.id,source:e.source,target:e.target,outcome:e.condition||e.branch_type||'Done',metadata:{branchType:e.branch_type,isDefault:Boolean(e.isDefault)}}))};}
